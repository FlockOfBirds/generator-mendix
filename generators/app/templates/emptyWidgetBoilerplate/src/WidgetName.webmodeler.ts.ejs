import { Component, createElement } from "react";
import WidgetName from "./components/WidgetName";
import { WidgetNameContainerProps } from "./components/WidgetNameContainer";

// tslint:disable-next-line class-name
export class preview extends Component<WidgetNameContainerProps> {
    render() {
        return createElement(WidgetName, this.props);
    }
}
