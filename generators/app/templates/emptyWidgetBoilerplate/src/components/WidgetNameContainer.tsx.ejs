import * as React from "react";
import { WidgetName } from "./WidgetName";

interface WrapperProps {
    class: string;
    mxObject?: mendix.lib.MxObject;
    style: string;
    readOnly: boolean;
}

interface WidgetNameContainerProps extends WrapperProps {
    sampleAttribute: string;
}

export default class WidgetNameContainer extends React.Component<WidgetNameContainerProps, {}> {
    render() {
        return <WidgetName />);
    }
}
