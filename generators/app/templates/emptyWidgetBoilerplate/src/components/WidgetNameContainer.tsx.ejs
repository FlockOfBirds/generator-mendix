import * as React from "react";
import WidgetName from "./WidgetName";
import { createElement } from 'react';

export interface WidgetNameContainerProps {
  sampleText: string;
}

export default class WidgetNameContainer extends React.Component<WidgetNameContainerProps, {}> {
  render() {
      return <WidgetName sampleText={this.props.sampleText} />
  }
}
