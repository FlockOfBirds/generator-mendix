import { Component, createElement } from "react";

interface WidgetNameProps {
  sampleText: string;
}

class WidgetName extends Component<WidgetNameProps, {}> {
  render() {
      return createElement("div", {}, `Hello ${this.props.sampleText}`);
  }
}

export default WidgetName;
